# to check status of docker daemon
sudo systemctl status docker

# list docker images
sudo docker images

# list of active containers
sudo docker ps


# to run a image in interactive mode
sudo docker run -it ubuntu:18.04 /bin/bash


## Images

# pull a image from a repo
sudo docker pull percona:8.0




